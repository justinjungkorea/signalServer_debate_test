const commonFn = require('./fn.common.service');
const coreServerEvent = function(socket, signalSocketio, coreSocketio, masterRedis, slaveRedis, sessionId){
    socket.on("knowledgetalk", function(data){

    });
}

module.exports = coreServerEvent;
